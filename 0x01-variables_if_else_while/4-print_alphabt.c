#include <stdio.h>
/**
 * main - prints the alphabet in lowercase.
 * Return: 0
 */
int main (void)
{
	char b = 'a';

	while (b <= 'z')
	{
		if (b != 'q' && b !='e')
		{ 
			putchar(b);
		}
		c++;
	}
	putchar('\n');
	return (0);
}
