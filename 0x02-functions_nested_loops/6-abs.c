#include "main.h"

/*
 *
 * _abs - 2-print_alphabet_x10.c
 * Description: Function that computes the absolute value
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
